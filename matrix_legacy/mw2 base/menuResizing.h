#pragma once
#include <iostream>
#include <stdio.h>

using namespace std;
//float scaleInfo, scaletitle, scaleName, scaleOpt, scaleSub;
//float infoX, infoY, shaderX, shaderY;
//float infoTextX, infoTextY, titleTextx, titleTextY, OptTextX, OptTextY, SubTextX, subTextY; 
float scaleInfo = 0.5;
float scaletitle = 1.2f;
float scaleName = 0.55;
float scaleOpt = 0.75;
float scaleSub = 0.75;

float infoX = 50;
float infoY = 187;
float shaderX = 50;
float shaderY = 187;

float infoTextX = 4;
float infoTextY = 6;
float titleTextx = 958;
float titleTextY = 100;
float OptTextX = 960;
float OptTextY = 147;
float SubTextX = 960;
float subTextY = 147;
/*
void resizingShit() {
	AllocConsole();
	freopen("CONIN$", "r", stdin);
	freopen("CONOUT$", "w", stdout);
	freopen("CONOUT$", "w", stderr);
	cout << "Thanks for Purchasing the revolutionary Indulge in MW2 menu! If your like me you like old school! This is the shit for you! Please read carefully below otherwise the menu will not work." << endl; 
	int sizeOpt; 
	

	cout << "Menu sizing options\n"; 
	cout << "1: original size (NIGHT + Gazzas Size). \n"; 
	cout << "2: Stealth (TJ + Vicary) Menu Size. \n";
	cout << "3: ACSM menu size. \n";
	cout << "4: 720p fullscreen optimised size. \n";
	cout << "5: 1080p fullscreen optimised size. \n";
	cout << "6: 4k fullscreen optimised size. \n";
	cout << "7: Customise to your liking (realtime not available as of yet). \n";

	cin >> sizeOpt; 


	// 1 = done; 
	if(sizeOpt == 1){ 
		scaleInfo = 0.5;
		scaletitle = 1.2f;
		scaleName = 0.55; 
		scaleOpt = 0.75; 
		scaleSub = 0.75;

		infoX = 16;
		infoY = 27;
		shaderX = 50;
		shaderY = 187;

		infoTextX = 4;
		infoTextY = 6;
		titleTextx = 50;
		titleTextY = 187;
		OptTextX = 960;
		OptTextY = 147;
		SubTextX = 960;
		subTextY = 147; 
	}
	else if (sizeOpt == 2) {
		scaleInfo = 0.5;
		scaletitle = 1.2f;
		scaleName = 0.55;
		scaleOpt = 0.75;
		scaleSub = 0.75;

		infoX = 16;
		infoY = 27;
		shaderX = 50;
		shaderY = 187;

		infoTextX = 4;
		infoTextY = 6;
		titleTextx = 958;
		titleTextY = 100;
		OptTextX = 960;
		OptTextY = 147;
		SubTextX = 960;
		subTextY = 147;
	}
	else if (sizeOpt == 3) {
		scaleInfo = 0.5;
		scaletitle = 1.2f;
		scaleName = 0.55;
		scaleOpt = 0.75;
		scaleSub = 0.75;

		infoX = 16;
		infoY = 27;
		shaderX = 950;
		shaderY = 50;
	}
	else if (sizeOpt == 4) {
		scaleInfo = 0.5;
		scaletitle = 1.2f;
		scaleName = 0.55;
		scaleOpt = 0.75;
		scaleSub = 0.75;

		infoX = 16;
		infoY = 27;
		shaderX = 950;
		shaderY = 50;
	}
	else if (sizeOpt == 5) {
		scaleInfo = 0.5;
		scaletitle = 1.2f;
		scaleName = 0.55;
		scaleOpt = 0.75;
		scaleSub = 0.75;

		infoX = 16;
		infoY = 27;
		shaderX = 950;
		shaderY = 50;
	}
	else if (sizeOpt == 6) {
		scaleInfo = 0.5;
		scaletitle = 1.2f;
		scaleName = 0.55;
		scaleOpt = 0.75;
		scaleSub = 0.75;

		infoX = 16;
		infoY = 27;
		shaderX = 950;
		shaderY = 50;
	}
	else if (sizeOpt == 7) {
		cout << "info scale: \n"; 
		cin >> scaleInfo; 
		cout << "title scale: \n";
		cin >> scaletitle; 
		cout << "name scale: \n";
		cin >> scaleName;
		cout << "opt scale: \n";
		cin >> scaleOpt;
		cout << "sub scale: \n";
		cin >> scaleSub;

		cout << "name coord: \n";
		cin >> infoX;
		cout << "info y coord: \n";
		cin >> infoY;
		cout << "shader X coord: \n";
		cin >> shaderX;
		cout << "shader Y coord: \n";
		cin >> shaderY;

		cout << "info X: \n";
		cin >> infoTextX;
		cout << "title Y: \n";
		cin >> infoTextY;
		cout << "Title X: \n";
		cin >> titleTextx;
		cout << "Title X: \n";
		cin >> titleTextY;
		cout << "Title Y: \n";
		cin >> OptTextX;
		cout << "Opt y: \n";
		cin >> OptTextY;
		cout << "Opt X: \n";
		cin >> SubTextX;
		cout << "Sub Y: \n";
		cin >> subTextY;
		
		
	}


}
*/